appId: com.beflow.petflow
name: PetFlow iOS capture password paste verification cleanup
---
- extendedWaitUntil:
    visible:
      id: "^auth-password-input$"
      focused: true
    timeout: 10000
- eraseText
- extendedWaitUntil:
    visible:
      id: "^auth-password-input$"
      focused: true
    timeout: 10000
